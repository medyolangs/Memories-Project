/* 
* using express routes 
import express from 'express'

const router = express.Router()

* Home route
* use controller method of MVC to store all the functions of your app
router.get('/', getRoute) 

export default router;
*/
